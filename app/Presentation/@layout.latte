<!DOCTYPE html>
<html>
<head>


	<meta charset="utf-8">
	{* <meta name="viewport" content="width=device-width"> *}
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="{$basePath}/css/bootstrap-icons.css">
	
	<link rel="stylesheet" href="{$basePath}/css/style.css">
	<link rel="stylesheet" href="https://bootswatch.com/5/spacelab/bootstrap.min.css">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">



	    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.0.0/css/all.css" />
    <!-- Google Fonts Roboto -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" />
    <!-- MDB -->
    <link rel="stylesheet" href="css/mdb.min.css" />

	<link rel="icon" type="image/x-icon" href="https://tvfordeaf.com/favicon.ico">

	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/cookieconsent@3/build/cookieconsent.min.css" />




	{* <title>{ifset title}{include title|stripHtml} | {/ifset}Nette Web</title> *}
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-5HERJSYQJK"></script>

{* <script n:syntax="off">
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-5HERJSYQJK');
</script> *}

{if $hasAnalyticsConsent}
    <!-- Google tag (GA4) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-5HERJSYQJK"></script>
    <script n:syntax="off">
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());

        gtag('config', 'G-5HERJSYQJK', { 'anonymize_ip': true });
    </script>
{/if}


</head>


<body>
	{* Header *}
	<div class="container" style="">
	{include 'header.latte'}
  <div class="" style=""><p style="color:blackgrey; font-family: font-family: Open Sans, sans-serif;" class="text-center">Skutečný hlas, skutečný dopad</p>
            </div>
		{foreach $flashes as $flash}
			<div class="alert alert-warning text-center">{$flash->message}</div>
		{/foreach}
	</div>

{* <div n:if="!$cookiesAccepted">
    {control cookie}
</div> *}

	{include content}
	{* <div n:snippet="flashes" id="flashes"> *}
	{* Flashes *}

      {if !$cookiesAccepted}
      <div id="cookie-consent" class="cookie-bar" n:if="!$cookiesAccepted">
    <div class="cookie-bar-content">
        <img src="{$basePath}/uploads/img/cookies.png" alt="Cookie Icon" class="cookie-icon">
        <p>
            Používáme soubory cookie, abychom zajistili co nejlepší uživatelský zážitek. Pokračováním v používání našeho webu s tím souhlasíte.
        </p>
        <div class="cookie-bar-buttons">
            <a n:href="acceptCookies! all" class="btn-accept" >Rozumím</a>
            <a n:href="acceptCookies! essentials" class="btn-accept">Pouze nezbytné</a>
            <a href="{link moreInfo!}" class="btn-info">Více informací</a>
        </div>
    </div>
</div>
    {/if}

	{include 'footer.latte'}



	<script>window.scrollTo({ top: 0, behavior: 'smooth' });</script>

	 <!-- MDB -->
    <script type="text/javascript" src="{$basePath}/js/mdb.umd.min.js"></script>
	
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
	<script src="https://unpkg.com/naja@2/dist/Naja.min.js"></script>
	<script src="https://unpkg.com/nette-forms@3/src/assets/netteForms.min.js"></script>
	<script src="https://unpkg.com/nette.ajax.js"></script>
	<script src="https://unpkg.com/htmx.org@2.0.4"></script>

	 {* <script>
        // Funkce pro nastavení cookie
        function setCookie(name, value, days) {
            let expires = "";
            if (days) {
                let date = new Date();
                date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
                expires = "; expires=" + date.toUTCString();
            }
            document.cookie = name + "=" + (value || "")  + expires + "; path=/; SameSite=Lax";
        }

        // Funkce pro získání cookie
        function getCookie(name) {
            let nameEQ = name + "=";
            let ca = document.cookie.split(';');
            for(let i=0;i < ca.length;i++) {
                let c = ca[i];
                while (c.charAt(0)==' ') c = c.substring(1,c.length);
                if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length,c.length);
            }
            return null;
        }

        // Zkontroluje cookie při načtení stránky
        document.addEventListener('DOMContentLoaded', (event) => {
            const consentBar = document.getElementById('cookie-consent-bar');
            const consentGiven = getCookie('cookie_consent');

            if (consentGiven === 'accepted') {
                consentBar.style.display = 'none';
                // Spustí Google Analytics, pokud souhlas již byl dán (pro případ, že by se nespustil v <head>)
                if (typeof gtag === 'function' && !window.gtagInitialized) {
                    gtag('config', 'G-5HERJSYQJK'); // Znovu nahraďte G-5HERJSYQJK vaším Measurement ID
                    window.gtagInitialized = true; // Zabrání vícenásobné inicializaci
                }
            } else {
                consentBar.style.display = 'block'; // Zobrazí lištu, pokud souhlas není
            }
        });

        // Funkce pro přijetí cookies
        function acceptCookies() {
            setCookie('cookie_consent', 'accepted', 365); // Souhlas na 365 dní
            document.getElementById('cookie-consent-bar').style.display = 'none';
            // Spustí Google Analytics, jakmile uživatel přijme cookies
            if (typeof gtag === 'function' && !window.gtagInitialized) {
                gtag('config', 'G-5HERJSYQJK'); // Nahraďte G-5HERJSYQJK vaším Measurement ID
                window.gtagInitialized = true;
            }
        }

        // Funkce pro odmítnutí cookies (v tomto případě je pouze skryje)
        function rejectCookies() {
            setCookie('cookie_consent', 'rejected', 365); // Lze uložit jako odmítnuto, ale pro jednoduchost je to jen skrytí
            document.getElementById('cookie-consent-bar').style.display = 'none';
            // Zde by se NEMĚLA spouštět Google Analytics ani jiné sledovací skripty
        }

        // Scroll to top on page load
        window.scrollTo({ top: 0, behavior: 'smooth' });
    </script>  *}

	{* Cookie z gitu *}

 {* <script src="https://cdn.jsdelivr.net/npm/cookieconsent@3/build/cookieconsent.min.js"></script>
<script n:syntax="off">
window.addEventListener("load", function(){
  window.cookieconsent.initialise({
    type: "opt-in",
    palette: {
      popup: { background: "#000" },
      button: { background: "#f1d600" }
    },
    theme: "classic",
    content: {
      message: "Tento web používá cookies.",
      allow: "Povolit vše",
      deny: "Odmítnout vše",
      link: "Nastavení",
      href: "www.facebook.com",
      target: "_self"
    },
    onInitialise: function (status) {
      if (this.hasConsented()) {
        enableAll();
      }
    },
    onStatusChange: function(status) {
      if (this.hasConsented()) {
        enableAll();
      } else {
        disableAll();
      }
    }
  });

  // vlastní funkce
  function enableAll() {
    loadGoogleAnalytics();
    loadMarketing();
  }

  function disableAll() {
    console.log("Vše zakázáno");
  }

  // příklad – načtení jen GA
  function loadGoogleAnalytics() {
    if (window.gaLoaded) return;
    window.gaLoaded = true;

    var s = document.createElement("script");
    s.src = "https://www.googletagmanager.com/gtag/js?id=G-5HERJSYQJK";
    s.async = true;
    document.head.appendChild(s);

    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', '5HERJSYQJK');
  }

  function loadMarketing() {
    console.log("Načtení marketingového trackingu…");
    // sem přidáš kód pro FB Pixel, reklamy apod.
  }

  // tady by sis udělal vlastní dialog pro detailní nastavení
  // a podle toho volal loadGoogleAnalytics() / loadMarketing()
});
</script> *}


<script>
    document.addEventListener('DOMContentLoaded', function() {
        const acceptButton = document.querySelector('.btn-accept');
        if (acceptButton) {
            acceptButton.addEventListener('click', function(e) {
                e.preventDefault();
                document.getElementById('cookie-consent').style.display = 'none';
                fetch(acceptButton.href);
            });
        }
    });
</script>

	
</body>

</html>
